intToColor(num)  from bbggrr returns #rrggbb

intToColor2(num)   from rrggbb returns #rrggbb

intToRGBA(num)   from rrggbb returns rgba(r,g,b,a)

function getTextWidth(s, fontInfo)

function getFontHeight(fontFamily, fontSize)

transformPalette(az_palette[]) -> ajoo_palette

class NodeStyle
class NodeLocation
class CompartmentStyle
class EdgeLocation
class EdgeStyle
