Run

gradle install

to create the corresponding .jar in webAppOS/dist/lib and shell scripts
in webAppOS/dist/bin.

If you wish to modify the ecore2java templates (.mtl files), then
you will need Acceleo to re-generate the compiled templates (.emtl files).
